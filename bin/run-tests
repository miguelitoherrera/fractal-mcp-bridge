#!/bin/bash
# Navigate to project root
cd "$(dirname "$0")/.."

# Run tests with JIT disabled and project paths included
NUMBA_DISABLE_JIT=1 PYTHONPATH="lib:src:$PYTHONPATH" \
    pytest --cov=src --cov=lib --cov-report=term-missing --cov-report=html tests "$@"
